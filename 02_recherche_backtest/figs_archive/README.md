# `figs_archive/` — figures reprises de la branche `presentation`

Six figures produites par des notebooks **archivés**, qu'aucun document de `main` ne citait, et que
`SLIDES_RECHERCHE.tex` a utilisées. Elles sont **copiées** ici, jamais déplacées (règle d'archive du
dépôt : le producteur d'une figure peut encore la réécrire sous le même nom).

Depuis que le deck est passé à 25 pages, **une seule est encore incluse**. Les cinq autres sont
gardées parce que leur notebook producteur est archivé : les reproduire coûterait plus cher que les
conserver.

| fichier | origine sur `presentation` | état |
|---|---|---|
| `m3_etf_manquant.png` | `_archive/figs_nb13/` | **utilisée** — ce que la carte sectorielle non datée perd |
| `scenarios_fifo.png` | `_archive/explications/` | orpheline — les 5 scénarios de vente qui définissent γ |
| `_explication_burr_covid_2020.png` | `_archive/explications/` | orpheline — le cas Burr, février 2020 |
| `concentration_sans_plafond.png` | `_archive/figs_nb14/` | orpheline — sans plafond, la plus grosse ligne monte à 87 % |
| `volume_divulgation.png` | `_archive/figs_nb14/` | orpheline — le marché ignore la publication (ρ 0,998) |
| `m3_etf_ou_agreger.png` | `_archive/figs_nb13/` | orpheline — où agréger : score ou poids |

⚠️ `_archive/figs_nb13/m3_nav.png` porte **le même nom** que `3_livrable_M3/figs/m3_nav.png` pour un
contenu différent — il n'est pas repris ici. Ne jamais dédoublonner par nom.
