# Environnement autonome — Pipeline Sénat 2025_test (Python 3.12)
# Versions épinglées sur le venv d'origine (semaine 1/.venv), validées.
# Installation :
#   python3.12 -m venv .venv
#   .venv/bin/pip install -U pip
#   .venv/bin/pip install -r requirements.txt
#   .venv/bin/python -m ipykernel install --user --name jupiter-senat-2025test \
#       --display-name "Jupiter Sénat 2025_test"

# Référentiel YAML + données + validation
pandas==3.0.3
pyyaml==6.0.3
requests==2.34.2
openpyxl==3.1.5
python-dotenv==1.2.2

# Parsing des rapports eFD HTML (pd.read_html) — indispensable à l'extraction
lxml==6.1.1
beautifulsoup4==4.15.0

# OCR papier (Claude Vision sur les images .gif eFD) + manip image
anthropic==0.112.0
pillow==12.2.0

# Pour exécuter les notebooks et enregistrer le kernel
ipykernel==7.3.0
nbconvert==7.17.1
