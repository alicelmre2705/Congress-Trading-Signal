.env
.DS_Store
__pycache__/
*.pyc
.ipynb_checkpoints/
# Config/session de l'outil Claude Code (permissions locales incluses) — hors dépôt
.claude/
# Environnement Python à la racine (recréable : python3.12 -m venv .venv && .venv/bin/pip install -e .)
.venv/
# Artefacts de build du package (refonte)
*.egg-info/
build/
dist/
*.log

# utilitaire perso (garder le Mac éveillé) — hors dépôt
keepawake

# Instantanés quotidiens de l'index du Clerk (common.first_seen --dry-run / local) :
# ~420 Ko par jour et par année, purement diagnostiques. Le journal d'horodatage
# (data/first_seen/) est la seule sortie du crawl qui doit être versionnée.
00_recuperation_donnees/data/house/index/daily/
