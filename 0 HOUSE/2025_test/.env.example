# Gabarit — copier en `.env` et renseigner les valeurs (le .env réel est gitignoré).
# Clé Anthropic (OCR Claude Vision + passe LLM ticker) : https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=
# Clé QuiverQuant (validation uniquement, optionnelle)
QUIVER_API_KEY=
